const cores = ['Vermelho','Branco','Preto','Laranja','Roxo','Amarelo','Azul'];

    let index = 0;

    while (index < cores.length){ //enquanto (condicao) {acao}
        console.log(`${index} - ${cores[index]}`);
        index ++;
    };
////////////////////////////////////////////////
    let contador = 0;

    while (contador<51) {

        console.log(`repeticao numero ${contador}`);

        contador ++;
    };
////////////////////////////////////////////////